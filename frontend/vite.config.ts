import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { VitePWA } from 'vite-plugin-pwa'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      registerType: 'autoUpdate',
      injectRegister: 'inline', // Inline script guarantees registration fires instantly on page load
      includeAssets: ['favicon.svg', 'apple-touch-icon.png'], // Tells Vite to explicitly track app icons
      manifest: {
        name: 'LogOfUs',
        short_name: 'LogOfUs',
        description: 'Document your love story together. A shared diary for couples.',
        theme_color: '#ffffff',
        background_color: '#ffffff',
        display: 'standalone',
        orientation: 'portrait',
        scope: '/',
        start_url: '/',
        icons: [
          {
            src: '/pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: '/pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          },
          {
            src: '/pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'any maskable'
          }
        ],
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,jpg,jpeg}'], // Included jpg/jpeg to cache your dashboard polaroids!
      },
      devOptions: {
        enabled: true,
        type: 'module'
      }
    }),
  ],
})